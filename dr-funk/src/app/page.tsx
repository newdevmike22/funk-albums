import Albums from "./../components/Albums";
import Header from "./../components/Header";

const Home = () => {
  return (
    <main className="container">
      <Header />
      <Albums />
    </main>
  );
};

export default Home;
